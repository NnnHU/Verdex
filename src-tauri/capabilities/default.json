{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Verdex window. Grants the HTTP plugin access to any http(s) URL (panels/judges → OpenAI-compatible gateways) and the FS plugin scoped access to appDataDir for config.json persistence.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "http://**" },
        { "url": "https://**" }
      ]
    },
    "fs:allow-appdata-read",
    "fs:allow-appdata-write",
    "fs:allow-appdata-meta"
  ]
}
